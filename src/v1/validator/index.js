const pageableV = require('./pageable');
const privilegeV = require('./privilege');
const roleV = require('./role');
const userV = require('./user');
const statusV = require('./status');
const deviceV = require('./device');

module.exports = { privilegeV, pageableV, roleV, userV, statusV, deviceV };
